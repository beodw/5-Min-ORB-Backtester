
"use client";

import React from 'react';
import type { PriceMarker as PriceMarkerType } from '@/types';

interface PriceMarkerProps {
  marker: PriceMarkerType;
}

export function PriceMarker({ marker }: PriceMarkerProps) {
  // This component will be adapted for lightweight-charts if needed,
  // or the logic will be handled directly in the main chart component.
  // For now, it's a placeholder.
  return null;
}
