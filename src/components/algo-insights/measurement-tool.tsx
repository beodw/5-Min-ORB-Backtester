
"use client";

import React from 'react';
import type { MeasurementTool as MeasurementToolType } from '@/types';

interface MeasurementToolProps {
  tool: MeasurementToolType;
}

export function MeasurementTool({ tool }: MeasurementToolProps) {
  // This component will be adapted for lightweight-charts if needed,
  // or the logic will be handled directly in the main chart component.
  // For now, it's a placeholder.
  return null;
}
